{
  "$comment": "Generated by scripts/sync-samples.ts from fixtures/captured/scenarios/ — do not edit by hand.",
  "samples": [
    {
      "file": "gap-heavy-1h.jsonl",
      "title": "Gap-heavy session at 1h",
      "description": "Claude-enacted scenario at the 1h TTL: four prompts against a toy project separated by six, eight and twelve minutes of idle time — the human-think-time shape where a 5m cache would lapse before every turn.",
      "source": "fixtures/captured/scenarios/gap-heavy-1h/session.jsonl",
      "claudeCodeVersions": [
        "2.1.251"
      ],
      "expectedRecommendation": "1h",
      "observedTtl": "1h",
      "requestCount": 9
    },
    {
      "file": "gap-heavy-5m.jsonl",
      "title": "Gap-heavy session at 5m",
      "description": "Claude-enacted scenario with the main cache TTL set to 5m (CLAUDE_CODE_PROMPT_CACHE_TTL=5m): the same four prompts and six/eight/twelve-minute gaps, so the log itself shows the prefix lapsing and being re-written before every turn.",
      "source": "fixtures/captured/scenarios/gap-heavy-5m/session.jsonl",
      "claudeCodeVersions": [
        "2.1.251"
      ],
      "expectedRecommendation": "5m",
      "observedTtl": "5m",
      "requestCount": 8
    },
    {
      "file": "model-switch.jsonl",
      "title": "Mid-session model and effort switch",
      "description": "Claude-enacted scenario at 1h: two turns on Opus 5, then /model to Sonnet 5, then effort dropped from high to medium — three hard cache resets that no elapsed time explains.",
      "source": "fixtures/captured/scenarios/model-switch/session.jsonl",
      "claudeCodeVersions": [
        "2.1.251"
      ],
      "expectedRecommendation": "5m",
      "observedTtl": "1h",
      "requestCount": 10
    },
    {
      "file": "tight-loop-5m.jsonl",
      "title": "Tight agent loop at 5m",
      "description": "Claude-enacted scenario: eight back-to-back prompts against a toy project with the main cache TTL set to 5m (CLAUDE_CODE_PROMPT_CACHE_TTL=5m). Every gap is well under five minutes, so the doubled 1h write price would buy nothing.",
      "source": "fixtures/captured/scenarios/tight-loop-5m/session.jsonl",
      "claudeCodeVersions": [
        "2.1.251"
      ],
      "expectedRecommendation": "5m",
      "observedTtl": "5m",
      "requestCount": 13
    }
  ]
}
